name = "{{SLUG}}"
compatibility_date = "2025-09-01"
pages_build_output_dir = "."

[assets]
directory = "."
not_found_handling = "404-page"
# binding = "ASSETS"  # uncomment when the sibling grows a Worker
#                       # entrypoint that needs to read its own
#                       # static assets programmatically.

[observability]
enabled = true

[placement]
mode = "smart"
